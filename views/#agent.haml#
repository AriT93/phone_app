#messages
  %h1 Messages:
%h1 Calls
#calls
  %ul
    - @coll.find().each do |row|
      %li.call{:id => "#{row['tn']}"}
        = "#{row['name']} :: #{row['tn']}"
        %a{:onclick => 'callCust()' rel="#call"}
          %img{:src => '/img/phone.png'}
#chat
